/**
 * Novedades de la versión ACTUAL del bundle. Se muestran una vez, tras aplicar una
 * actualización por aire (OTA). Como corren en el bundle nuevo, describen lo que trae ese bundle.
 *
 * IMPORTANTE: actualizar este texto en cada `eas update` (es lo que verá el usuario como
 * "novedades"). Frases cortas, una por línea.
 */
export const NOVEDADES = [
  "Mejor deteccion de lo que admite tu robot (leyendo sus capacidades, como Home Assistant).",
  "El sondeo de Diagnostico ahora incluye esa info para seguir afinando.",
].join("\n");
